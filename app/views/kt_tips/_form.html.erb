<%= form_with(model: kt_tip, local: true) do |form| %>
  <% if kt_tip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kt_tip.errors.count, "error") %> prohibited this kt_tip from being saved:</h2>

      <ul>
      <% kt_tip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :topic %>
    <%= form.text_field :topic %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :written_on %>
    <%= form.datetime_select :written_on %>
  </div>

  <div class="field">
    <%= form.label :likes %>
    <%= form.number_field :likes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
